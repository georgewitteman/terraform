image:
  file: .gitpod.Dockerfile

tasks:
  - name: Initialize Terraform
    command: |
      terraform init
      terraform plan && exit 0

vscode:
  extensions:
    - hashicorp.terraform
    - ms-azuretools.vscode-docker
